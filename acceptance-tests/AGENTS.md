# Agent Instructions

## Test authoring rules

- When implementing tests, include inline documentation (for example method Javadoc) that states test intent.
- Use Awaitility when waiting for asynchronous processing.
- Keep test code linear; avoid if/else, loops, and other branching blocks in test bodies.
- When editing tests, verify that existing inline test documentation is still correct and update it when needed.
